import 'package:flutter/material.dart';

const kRadius = 15.0;

const kBottomSheetRadius = BorderRadius.only(
  topLeft: Radius.circular(kRadius),
  topRight: Radius.circular(kRadius),
);

const kNasaLogoColor = Color.fromRGBO(219, 54, 45, 1);
